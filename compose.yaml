services:
  uptime-kuma:
    image: louislam/uptime-kuma:2
    restart: unless-stopped
    volumes:
      - ./uptime-data:/app/data
  frontend:
    build: website/
    restart: unless-stopped
    env_file: "website/.env"
  backend:
    build: api/
    links:
      - db
    restart: unless-stopped
    env_file: "api/.env"
  caddy:
    image: caddy
    restart: unless-stopped
    cap_add:
      - NET_ADMIN
    ports:
      - "80:80"
      - "443:443"
      - "443:443/udp"
    volumes:
      - $PWD/conf:/etc/caddy
      - caddy_data:/data
      - caddy_config:/config
      - /home/hazel/hazelpersonal:/var/www/hazel.legiti.dev
      - /home/trplnr/legitiawards/build:/var/www/legitiawards.legiti.dev
  db:
    image: mongo
    restart: unless-stopped
    volumes:
      - ./data:/data/db
    ports:
      - "27017:27017"
  postgres:
    image: postgres
    restart: unless-stopped
    volumes:
      - ./pgdata:/var/lib/postgresql
    ports:
      - "5432:5432"
volumes:
  caddy_data:
  caddy_config:
